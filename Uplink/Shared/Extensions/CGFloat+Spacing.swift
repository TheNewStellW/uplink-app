import Foundation

extension CGFloat {
    /// Icon-to-label gap, tight inline spacing
    static let spacing2: CGFloat = 2
    /// Within a component (e.g. badge padding)
    static let spacing4: CGFloat = 4
    /// Between related elements in a row
    static let spacing8: CGFloat = 8
    /// List row vertical padding, inner content padding
    static let spacing12: CGFloat = 12
    /// Between sections, outer content padding
    static let spacing16: CGFloat = 16
    /// Between major layout regions
    static let spacing24: CGFloat = 24
}
